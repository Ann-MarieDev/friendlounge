specVersion: "v1beta"
serviceId: "friendlounge"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "friendlounge-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
